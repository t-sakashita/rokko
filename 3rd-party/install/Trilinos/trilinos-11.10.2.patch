diff -crN trilinos-11.10.2-Source.orig/packages/ifpack/src/Ifpack_OverlappingPartitioner.cpp trilinos-11.10.2-Source/packages/ifpack/src/Ifpack_OverlappingPartitioner.cpp
*** trilinos-11.10.2-Source.orig/packages/ifpack/src/Ifpack_OverlappingPartitioner.cpp	2014-08-15 01:47:19.000000000 +0900
--- trilinos-11.10.2-Source/packages/ifpack/src/Ifpack_OverlappingPartitioner.cpp	2014-10-01 10:05:52.000000000 +0900
***************
*** 224,230 ****
  
  	  // has this column already been inserted?
  	  std::vector<int>::iterator
! 	    where = find(tmp[part].begin(), tmp[part].end(), col);
  
  	  if (where == tmp[part].end()) {
  	    tmp[part].push_back(col);
--- 224,230 ----
  
  	  // has this column already been inserted?
  	  std::vector<int>::iterator
! 	    where = std::find(tmp[part].begin(), tmp[part].end(), col);
  
  	  if (where == tmp[part].end()) {
  	    tmp[part].push_back(col);
diff -crN trilinos-11.10.2-Source.orig/packages/ifpack/src/Ifpack_OverlappingRowMatrix.cpp trilinos-11.10.2-Source/packages/ifpack/src/Ifpack_OverlappingRowMatrix.cpp
*** trilinos-11.10.2-Source.orig/packages/ifpack/src/Ifpack_OverlappingRowMatrix.cpp	2014-08-15 01:47:18.000000000 +0900
--- trilinos-11.10.2-Source/packages/ifpack/src/Ifpack_OverlappingRowMatrix.cpp	2014-10-01 10:07:13.000000000 +0900
***************
*** 106,112 ****
        int_type GID = (int_type) ColMap->GID64(i); 
        if (A().RowMatrixRowMap().LID(GID) == -1) { 
          typename std::vector<int_type>::iterator pos 
!           = find(ExtElements.begin(),ExtElements.end(),GID); 
          if (pos == ExtElements.end()) { 
            ExtElements.push_back(GID);
            list[count] = GID; 
--- 106,112 ----
        int_type GID = (int_type) ColMap->GID64(i); 
        if (A().RowMatrixRowMap().LID(GID) == -1) { 
          typename std::vector<int_type>::iterator pos 
!           = std::find(ExtElements.begin(),ExtElements.end(),GID); 
          if (pos == ExtElements.end()) { 
            ExtElements.push_back(GID);
            list[count] = GID; 
diff -crN trilinos-11.10.2-Source.orig/packages/ml/src/MatrixFree/ml_MatrixFreePreconditioner.cpp trilinos-11.10.2-Source/packages/ml/src/MatrixFree/ml_MatrixFreePreconditioner.cpp
*** trilinos-11.10.2-Source.orig/packages/ml/src/MatrixFree/ml_MatrixFreePreconditioner.cpp	2014-08-15 01:48:00.000000000 +0900
--- trilinos-11.10.2-Source/packages/ml/src/MatrixFree/ml_MatrixFreePreconditioner.cpp	2014-10-01 10:18:44.000000000 +0900
***************
*** 550,556 ****
        // FIXME: use hash??
        const int& GCID = Graph.ColMap().GID(Indices[k]);
  
!       iter = find(aggregates[AID].begin(), aggregates[AID].end(), GCID);
        if (iter == aggregates[AID].end())
          aggregates[AID].push_back(GCID);
      }
--- 550,556 ----
        // FIXME: use hash??
        const int& GCID = Graph.ColMap().GID(Indices[k]);
  
!       iter = std::find(aggregates[AID].begin(), aggregates[AID].end(), GCID);
        if (iter == aggregates[AID].end())
          aggregates[AID].push_back(GCID);
      }
diff -crN trilinos-11.10.2-Source.orig/packages/ThreadPool/src/TPI.c trilinos-11.10.2-Source/packages/ThreadPool/src/TPI.c
*** trilinos-11.10.2-Source.orig/packages/ThreadPool/src/TPI.c	2014-08-15 01:47:40.000000000 +0900
--- trilinos-11.10.2-Source/packages/ThreadPool/src/TPI.c	2014-10-01 10:26:36.000000000 +0900
***************
*** 68,74 ****
  #define THREADPOOL_CONFIG "PTHREAD SCHED_YIELD"
  
  #elif	defined( __linux__ ) && \
! 	defined( __GNUC__ ) && ( 4 <= __GNUC__ )
  
  #define THREADPOOL_CONFIG "PTHREAD SCHED_YIELD ATOMIC_SYNC"
  
--- 68,74 ----
  #define THREADPOOL_CONFIG "PTHREAD SCHED_YIELD"
  
  #elif	defined( __linux__ ) && \
! 	defined( __GNUC__ ) && ( 4 <= __GNUC__ ) && !defined(__fcc_version)
  
  #define THREADPOOL_CONFIG "PTHREAD SCHED_YIELD ATOMIC_SYNC"
  
