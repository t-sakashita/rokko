set(PROG dgesvd dgetri dgetrs dlange slange dsyev dsyev_r zheev)
foreach(name ${PROG})
  set(target_name example_lapack_${name})
  add_executable(${target_name} ${name}.c)
  set_target_properties(${target_name} PROPERTIES OUTPUT_NAME ${name})
  target_link_libraries(${target_name} rokko)
  add_test(${name} ${name})
endforeach(name)
unset(PROG)

set(PROG gesvd gesvd_z getrs getrs_z heev heev_c lange orgqr ungqr syev syev_s steqr)
foreach(name ${PROG})
  set(target_name example_lapack_${name})
  add_executable(${target_name} ${name}.cpp)
  set_target_properties(${target_name} PROPERTIES OUTPUT_NAME ${name})
  target_link_libraries(${target_name} rokko)
  add_test(${name} ${name})
endforeach(name)
unset(PROG)

if(BUILD_Fortran_BINDING)
  set(PROG dlange_f slange_f)
  foreach(name ${PROG})
    set(target_name example_lapack_${name})
    add_executable(${target_name} ${name}.f90)
    set_target_properties(${target_name} PROPERTIES OUTPUT_NAME ${name})
    target_link_libraries(${target_name} rokko)
    add_test(${name} ${name})
  endforeach(name)
  unset(PROG)
endif(BUILD_Fortran_BINDING)
